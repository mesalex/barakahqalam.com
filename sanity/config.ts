// Sanity config - will be used with Sanity CLI
// This is a placeholder config file

const config = {
  name: 'barakahqalam',
  title: 'Barakah Qalam CMS',
  projectId: 'your-project-id',
  dataset: 'production',
  plugins: [],
  schema: {
    types: [],
  },
};

export default config;